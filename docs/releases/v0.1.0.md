# ToxicJoin v0.1.0

This is the first immutable, evidence-bound ToxicJoin release.

## Immutable identity

- Tag: `{{TAG}}`
- Commit: `{{SOURCE_SHA}}`
- Release Manifest SHA-256: `{{MANIFEST_SHA256}}`
- Evidence Index SHA-256: `{{EVIDENCE_INDEX_SHA256}}`

## Verified release surface

The attached Release Manifest binds CI, benchmark, PPMC, supply-chain, container, security, portability, Live DataHub, production browser E2E, topology, and durable evidence retention to the exact release commit.

The release remains truthful about current boundaries:

- canonical disclosure topology is `SINGLE_NODE`;
- multi-replica stateful privacy support is not claimed;
- PostgreSQL is not canonical product behavior;
- hosted Replay is deterministic replay and is not represented as live execution;
- Devpost submission is not claimed by this release.

## Attached verification material

The release assets include the exact Release Manifest, final evidence index, four CycloneDX SBOMs, reproducibility instructions, deterministic evidence bundle, and SHA-256 checksums.
