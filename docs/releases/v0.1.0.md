# ToxicJoin v0.1.0

This is the first exact-SHA, evidence-bound ToxicJoin release.

This repository file is the Phase 9 publication template. The workflow substitutes the placeholders
below in the attached `RELEASE_NOTES.md`; unresolved placeholders are rejected before publication.
Phase 9 verifies the tag, Release, and asset checksums at publication time. This wording does not
claim that GitHub's immutable-release setting is enabled or that the annotated tag is
cryptographically signed.

## Release identity

- Tag: `{{TAG}}`
- Commit: `{{SOURCE_SHA}}`
- Release Manifest SHA-256: `{{MANIFEST_SHA256}}`
- Evidence Index SHA-256: `{{EVIDENCE_INDEX_SHA256}}`

## Verified release surface

The attached Release Manifest binds CI, benchmark, PPMC, supply-chain, container, security, portability, Live DataHub, production browser E2E, topology, and durable evidence retention to the exact release commit.

The release remains truthful about current boundaries:

- canonical disclosure topology is `SINGLE_NODE`;
- multi-replica stateful privacy support is not claimed;
- PostgreSQL is not canonical product behavior;
- the [Current Public Demo](https://toxicjoin-public-demo.onrender.com/) uses a synthetic
  fixture through the real protected execution path; it is not a mock and contains no
  institutional data or credentials;
- Render runtime state is temporary and may reset after sleep or restart, so it is not a
  durable audit record;
- Live DataHub is established separately by Exact-SHA CI evidence, not by the public
  Render service;
- the public fixture demo is not represented as Live DataHub execution;
- video publication and Devpost synchronization are not claimed by this release.

## Attached verification material

The release assets include the exact Release Manifest, final evidence index, four CycloneDX SBOMs, reproducibility instructions, deterministic evidence bundle, and SHA-256 checksums.
